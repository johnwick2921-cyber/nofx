# VL Production Deployment
# 用户部署专用 - 使用官方预构建镜像
#
# 一键部署命令:
# curl -fsSL https://raw.githubusercontent.com/NoFxAiOS/nofx/main/install.sh | bash
#
# 或手动部署:
# curl -O https://raw.githubusercontent.com/NoFxAiOS/nofx/main/docker-compose.prod.yml
# docker compose -f docker-compose.prod.yml up -d

services:
${1}vl:
    image: ghcr.io/nofxaios/nofx/nofx-backend:latest
    container_name: vl-trading
    restart: unless-stopped
    stop_grace_period: 30s
    ports:
      - "${VL_BACKEND_PORT:-${NOFX_BACKEND_PORT:-8080}}:8080"
    volumes:
      # Data directory for database and logs
      - ./data:/app/data
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - .env
    environment:
      - TZ=${TZ:-Asia/Shanghai}
      - AI_MAX_TOKENS=8000
    networks:
      - vl-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  vl-frontend:
    image: ghcr.io/nofxaios/nofx/vl-frontend:latest
    container_name: vl-frontend
    restart: unless-stopped
    ports:
      - "${VL_FRONTEND_PORT:-${NOFX_FRONTEND_PORT:-3000}}:80"
    networks:
      - vl-network
    depends_on:
${1}- vl
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

networks:
  vl-network:
    driver: bridge

# One arm-state predicate; leg 4 separates authorization from placement

Branch **fix/arm-state-predicate**, base **8941ec68612cc019edc3002b999272ab2ed20516**, claimed by `arm-state-0b955fbc/root[unlisted]`. Number assigned only at merge. This is the next code task after brand-visible; no deployment or live database write is part of this implementation receipt.

## Finding and implementation

[A] `trader/one_contract.go` had the full terminal predicate, but `armedActually` used a shorter terminal list, store readers used positive state lists, and the boot census added four named states. The boot sweep omitted cancel_pending. Leg 4 compared every non-terminal ledger row with the broker, including authorizations never sent. The earlier brand-visible observation (rows 133/134/135 versus broker snapshot 14449) demonstrated the resulting broker 1 / ledger 3 mismatch; the two extras were unplaced authorizations, not missing broker orders.

The original predicate moves to **store.IsTerminalArmState**, avoiding a store→trader import cycle. `store/arm_state.go` contains the sole classification table. `TerminalArmStateSQL()` is generated by applying that Go predicate to its known state names; the non-terminal fragment is its negation. Go and SQLite normalize casing and the same Unicode whitespace set. NULL/empty/unknown remain non-terminal, matching the original Go predicate rather than silently treating an unfamiliar row as finished.

Store live-row ordering, non-terminal selection, pre-boot selection, the boot census, one-contract logic, authored-arm logging and remaining arm readers now use the shared predicate. Lifecycle actions keep explicit single-state restrictions where appropriate. Unknown states therefore appear in liveness reads; UpsertArm conservatively refuses rewriting an unknown non-terminal row rather than treating it as a finished placement. A received rejection may settle a non-terminal row linked by its exact trader/signal; the update remains scoped and cannot resurrect terminal rows.

Leg 4 consumes the production ledger renderer, excludes terminal rows, and separates **armed + empty signal** into `armed_unplaced` plus a distinct informational detail line. Remaining rows count as working/unconfirmed. Pending placement is never downgraded to an authorization, including malformed rows missing identity. The broker book remains the comparison source. Two authorized arms with broker zero PASS; pending with broker zero FAIL; broker one with no ledger placement FAIL. The existing explicit pre-first-snapshot ledger fallback and broker freshness limit are unchanged by this task.

The existing record boot line now includes a separately read `arm placement census: N working/unconfirmed · M armed (authorized, no signal id; informational)`. Ledger failure prints UNKNOWN. API help, Guide and SYSTEM-MAP are updated in the implementation commit. GUIDE_BUILT_REV will be stamped from the clean built binary at an authorized deployment; this branch does not claim to be running.

## Every-reader boundary

`go run ./cmd/arm-state-sql` prints the canonical non-terminal SQL; `-terminal` prints its terminal form. The command opens no database. Python watches/audits can import `scripts/arm_state.py`, which evaluates the Go-derived SQL rather than owning a Python state list. The executable q51 audit and arm replay now use these sources. Captured JSON/CSV outputs and historical prose remain historical receipts; they are not silently re-generated or rewritten to claim new observations. The replay has synthetic `none`/`refused` states outside the ledger domain, handled explicitly around the shared arm predicate. Its old captured outcomes are not claimed revalidated here.

The source guard scans executable Go/Python/shell/SQL wherever located, including report directories and untracked files. It rejects SQL state IN lists, literal SQL state sets, Go classification switches/boolean sets and OR-chained arm State comparisons. String-to-string display maps and plan-lifecycle fixtures are not arm classifiers. NT8's broker-state classifier and the historical broker-order reaper replay are a different domain and are explicitly excluded; applying an arm-terminal predicate to broker states would itself be a bug. Like a grep test, this guard checks recognized source forms, not every possible obfuscation of arbitrary code.

## Scope correction: boot sweep

Centralizing selection means ListPreBoot now includes **all canonically non-terminal rows**, including cancel_pending and unknown values. No adoption behavior was added: the sweep still skips rows without signal ids and cancels selected prior-process placed rows, without checking scenario validity. The owner-named adopt-or-cancel-by-scenario-validity wave remains separate. This change neither grants a cutover override nor edits a gate to accept a specific live order.

## Validation

[A] [RED before production edits](2026-09-09-arm-state-predicate-data/red.txt): two authorizations caused `broker 0 vs ledger 2 — MISMATCH`; the pending-row detail did not say unconfirmed; rejected/superseded/shadowed/canceled rows were reported armed. Focused store/trader tests now PASS, including SQL/Go parity through real store readers, all requested gate cases, malformed/unknown states, API count propagation and the actual boot census.

[Mutation receipts](2026-09-09-arm-state-predicate-data/mutations.json): hiding pending placement and ignoring a broker-only order each changed FAIL to an incorrect PASS and were caught by the production-path pin. A copied SQL list in a newly created audit script made the repository guard fail. All three mutations returned test exit 1. Full-suite receipts follow as they complete. All deliberate mutations are restored before validation/commit. No live order, account binding, database row or running process is changed.

## Source freshness and publication

Basis `git log -1` outputs at acceptance: one_contract.go `7797b36f 2026-09-07T10:25:18-05:00 fix(cancel): D4 — every cancel sender is guarded, and the census proves it`; AUDIT-CHECKLIST `8941ec68 2026-09-09T21:27:01-05:00 merge cd5b59ac into the combined boot head — 104 session risk`; SYSTEM-MAP `a59b6c9d 2026-09-09T21:26:37-05:00 merge 35fa69cc into the combined boot head — 103 candidates`; Guide guards `27e062ea 2026-09-09T14:55:48-05:00 merge dev (W5) + correct the three sentences this boot makes false`.

Main was already owned by the combined-boot lane, with dirty RELEASE/Guide deployment metadata. This task used an isolated locked worktree and did not alter or take over that main tree. Merge/build/boot claims require fresh evidence after that lane releases the lock.

[Per-file source freshness](2026-09-09-arm-state-predicate-data/source-freshness.json) records the last committed change for every modified existing file before this implementation commit.

## Validation follow-up

[A] At implementation commit **715f361a3be80914a31672f11c8264c7f6f08524**, full `go test ./...` PASS; frontend **58 files / 414 tests PASS**; TypeScript PASS; Python helper smoke PASS. The pre-commit hook initially could not spawn ESLint because the new worktree lacked installed dependencies; linking the existing ignored web/node_modules let the unchanged hook pass. No hook was bypassed.

[A] Review then found a Go/SQLite Unicode casing discrepancy: `IsTerminalArmState("FİLLED")=true`, but SQLite LOWER alone returned false. The new store-reader variant reproduced it as `SQL/Go disagree for "expİred"` ([RED](2026-09-09-arm-state-predicate-data/unicode-red.txt)). The SQL generator now derives non-ASCII-to-ASCII replacements from Go's Unicode case ranges, as well as its Unicode whitespace set. The parity pin passes after correction. The Go classifier is unchanged. Final integrated-head Go validation follows this correction.

# Frontend state repairs — isolated source batch

Base63968be; branch fix/repo-audit-web-state-20260913, claim3ca1c809. Source changes and offline fixtures only. No owner settings/accounts/API keys, app server,8080E2E, NT8, live data or deployment touched. Root owns API counterpart, guide/content/revision and final merged-head validation.

- EditSheet save/delete bind the edit-opening plan ID/version/overlay version through the real API client. SessionPlanCard snapshots the revision when selecting a row. Poll updates preserve the draft; missing revision refuses at the client, and HTTP409 leaves inputs intact. Requires root's GET today fields and atomic expected-revision POST validation. Before regression failed because submitted revision was undefined; after11 P5 tests pass. A previous worker message called delete 'undo'; source has save/delete, no undo caller.
- Agent stream implementation moved from page to lib/agentStream so authentication changes can abort it even while the page is unmounted. Per-request controller and user ownership gate every async result; cleanup owns only its reader/loading state. Storage failures cannot strand loading. Auth transitions reset chat and remount a session-owned SWR cache. Malformed stored users finish hydration as guest. Guest/legacy history is retained separately and is never silently attributed to an authenticated user. Existing storage-key strings remain unchanged; the protected byte hash is advanced with a provenance comment because the owner authorized this behavior change.
- SSE event/data state spans arbitrary UTF-8 chunks, CRLF and EOF; deltas append as actual chunks (agent/stream_text.go emits chunks), including repeats. Offline production-stream tests cover split headers, old-user completion, same-user stop/restart, quota failure, repeated deltas. Parser test covers every split position of Unicode/multiline/final-line fixture. These are synthetic regressions, not a captured live disclosure or network trace. Abort does not claim backend tools have stopped.
- ChartTabs uses the selected market's exchange unless the supplied exchange belongs to that market. Existing production-call-site test now inspects exchange as well as symbol: before Crypto under NT received ninjatrader; after receives binance.
- Two-stage key input resets on close/reopen/context change and invalidates clipboard/timer continuations. Regression closes/reopens during delayed stage transition and proves empty stage1. Model configuration prefers saved instance fields over catalog defaults, does not repopulate redacted secrets, initializes once per edited identity and clears drafts on provider selection. NT exchange edit initializes saved directory/instrument/quantity; other secrets clear when changing selected exchange. Model key input no longer requires a replacement secret for ordinary edits. Wallet validation/test/generation responses are bound to the active key generation; stale QR/address data is ignored and backup text clears when key changes. These additional form paths are source-fixed and type/build checked, not all individually reproduced.
- Registry panel validates HTTP/payload shape, clears old identity data and recovers from error. System config retries failed requests and protects the cache from invalidated request completion. Initial onboarding failure renders error+retry. FAQ English/Chinese/Indonesian no longer recommends deleting SQLite WAL/SHM; protection guidance describes NT8 SIM broker state. Wallet key text now accurately says configured-server validation/signing instead of never uploaded.

Validation before dependency updates:
- Exact package-lock hash matched shared installed dependencies before using a symlink; no install/update against shared modules.
- Full Vitest first run:432passed,8skipped, storage hash guard failure plus brand-visible Go spawn EPERM. Hash fixed for authorized semantic change.
- Final ordinary suites:68files,434tests passed; brand-visible excluded and checked separately because its synthetic Go fixture needs process/cache escalation. Separately escalated synthetic branding fixture:1file/8tests passed; together442tests across69files.
- npm run build passed (TypeScript+Vite), with existing large-chunk and stale browsers-data warnings.
- One initial wrong-root invocation failed setup resolution for all discovered files and ran zero tests. Corrected cwd is this worktree/web; no E2E runner was invoked.
- Detailed raw logs retained locally at /tmp/nofx-web-{overlay-before,overlay-after,chart-before,focused,focused2,suite,suite-final,brand,build}.log. Reports do not claim a final merged Go/web suite pass.

Residuals explicitly outside this batch: AdvancedChart request-generation/marker ownership, legacy comparison chart, corrected-only PositionHistory aggregation, bulk model payload replay/extra-model knob loss, model submit await/UI state, account-name backend persistence, all historical review findings not listed above, and end-to-end broker/runtime/backup evidence. Root controls further prioritization; dormant paths remain reports. Dependency alerts handled in a separate compatible-lock change after source commit.

# One setup + the recorded follow — dispatch 102 (2026-09-10/11)

**Branch** `fix/one-setup` · lane `one-setup-2bdef526/nofx-59[3f0200]` · claim
`964b4e25` 21:45:37 CT 09-10 · base at build: dev `499e4f83` (the rollback
marker) · **merge/boot: see §F (filled at the kill)**.

**Specs built from** (`git log -1 --`, in the worktree at the base):
- `docs/superpowers/VL-TRADING-RULEBOOK-v1.md` — `6509c2e8 2026-09-10 18:53:54 -0500 test(identity): pin recording authority and preserve detector output parity`
- W2 report `2026-09-10-fade-permission.md`, 105 report `2026-09-10-scenario-level-identity.md`, roll `2026-09-10-contract-roll.md`, bar-source `2026-09-10-bar-source.md` — all at `499e4f83`.
- **Round 17 (the two-sided level) is NOT on dev** at build time: no `research/2026-09-10-two-sided-level/`, no "Mesfin"/"two-sided" anywhere on dev. The dispatch's own summary of its verdicts is the citation for every `[T]` label here; the report is pinned by sha the moment it lands.

## The premise inverted, first (A17)

Three of the dispatch's premises did not survive Section C:

1. **"Whichever fires first" is not measurable from the record W1 built** — W1's touch→scenario link resolved **0 of 999** live rows since its boot. Cause, reproduced: the recorder's anchors come from `kernel.ParsePlanDoc(latest.Doc)` re-run on the STORED doc with the parser's default cap; the bound strategy seats 12 levels; `ParsePlanDoc ERROR: too many levels: 12 (max 8)` → nil anchors → basis `unresolved:no_scenario_at_seat` on 889 rows. **Class 120.** A15 for 101; this wave links scenario→episode by price inside the map's 3.0-pt width and says so on the row.
2. **The obstacle target arms almost nothing at the live floor.** `arm_rr=2.0` (boot line 00:45). On the C5 population 49 of 53 authored first obstacles sit under 2R (median 0.94R); on the 24 recent ledger rows with an obstacle, 0 reach 2.0 (max 1.98). D3 as dispatched means the book arms only when the first obstacle is ≥ 2R away — about 4 of 53 authored scenarios since 09-08. Built as dispatched, counted `obstacle_below_floor`; the number is the owner's to rule on.
3. **The tape was two-scaled at both the roll boot (0070fc79, 22:39) and the bar-source boot (abc420f8, 00:15)** — C8 was a STOP twice; it passed only after the rollback (`499e4f83`, "NT8 had not rolled; the AddOn had"), verified on the ring itself at 00:46. Evidence in `-data/c8-ring-*.json`.

## C — THE EVIDENCE (snapshot `~/nofx-analysis/one-setup/data-copy.db` 00:46:54 CT, running rev 0070fc79)

**C1 — the book arms whichever fires first.** Code [A]: `trader/armed_executor.go` iterates `for _, sc := range doc.Scenarios` and arms every scenario that passes the gates — no selection among scenarios. Data (touches after plan birth, scenario anchor within 3.0 pt): 78 plans with ≥2 armable scenarios; **44 never reached the ledger**; 31 had no resolvable scenario-level touch after birth; **3 with a resolvable first touch, and in all 3 the first-touched scenario armed** (09-09 ASIA v1 S1 18:15; 09-10 LONDON v3 S1 02:29; 09-10 LONDON v5 S1 04:40). n=3 — descriptive. Ids in `-data/c1_refined.json`.

**C2 — reject is the only positive cell** (`trader_positions` CLOSED, `pnl_corrected` only; NULL excluded and counted; condition via `trade_excursions` → `armed_orders` → plan doc):

| condition | n | Σ pnl_corrected | mean | NULL excluded | position ids |
|---|---:|---:|---:|---:|---|
| `reject` | 39 | 314.0 | 8.05 | 3 | 521, 523, 524, 529, 533, 535, 536, 537, 538, 544, 547, 548, 549, 550, 551, 552, 553, 554, 560, 564, 565, 567, 569, 570, 575, 581, 582, 584, 585, 587, 591, 593, 596, 597, 598, 599, 602, 603, 604 |
| `sweep_reclaim` | 12 | -325.5 | -27.12 | 1 | 368, 559, 561, 568, 578, 583, 586, 592, 594, 600, 601, 605 |
| `reclaim` | 5 | -436.5 | -87.3 | 0 | 525, 531, 540, 541, 588 |
| `breakout_retest` | 9 | -581.5 | -64.61 | 0 | 528, 534, 556, 557, 558, 562, 563, 589, 590 |
| `acceptance` | 6 | 4.57 | 0.76 | 0 | 522, 526, 527, 532, 542, 543 |
| `hold` | 1 | 168.0 | 168.0 | 0 | 555 |
| `breakup_continue` | 1 | -86.5 | -86.5 | 0 | 595 |
| `UNLINKED` | 71 | -248.75 | -3.5 | 117 | 288, 355, 356, 357, 358, 359, 360, 361, 362, 363, 364, 365, 366, 367, 369, 370, 371, 372, 373, 374, 375, 376, 377, 379, 381, 383, 384, 393, 416, 419, 425, 426, 430, 431, 433, 436, 437, 439, 440, 442, 444, 448, 452, 467, 468, 470, 472, 475, 481, 488, 489, 500, 501, 502, 503, 506, 508, 509, 512, 515, 516, 517, 530, 539, 545, 546, 566, 571, 573, 574, 580 |

`reject` is the only meaningfully positive cell (+$314.00, n=39). The dispatch cited +$586 n=31 (the strategy audit's cohort to 09-08); this snapshot runs to 09-10 and the cell is smaller. n=39 is a reason to isolate, not a verdict.

**C3 — the best level near price** (13 planner reads since W-TF's boot 17:11 CT 09-10; the pool read's candidates merged at 3.0 pt, ranked grade first, distance second):

| read | plan v | price | top (price · grade · names) | scenarios on top | armed (state) | armed on top |
|---|---|---:|---|---|---|---|
| 09-10 17:30 | 1 | 29120.0 | 29117.25 A ONL | — | S1:cancelled | — |
| 09-10 18:14 | 2 | 29110.0 | 29106.87 A VWAP−1σ | — | — | — |
| 09-10 18:46 | 2 | 29104.25 | 29101.75 A EQL·15m | — | — | — |
| 09-10 18:49 | 2 | 29098.75 | 29101.75 A EQL·15m | — | — | — |
| 09-10 19:27 | 3 | 29138.0 | 29132.50 A EQL·1h | — | S2:cancelled,S1:cancelled | — |
| 09-10 20:00 | 4 | 29159.25 | 29161.12 A OB(bull)·1h | — | — | — |
| 09-10 20:30 | 5 | 29129.5 | 29132.50 A EQL·1h | — | — | — |
| 09-10 21:06 | 6 | 29124.75 | 29132.50 A EQL·1h | — | — | — |
| 09-10 21:17 | 6 | 29132.0 | 29147.47 A VWAP+1σ | S1 | — | — |
| 09-10 22:00 | 7 | 29089.5 | 29104.25 A SWG-L·5m | — | — | — |
| 09-10 22:41 | 8 | 29075.25 | 29081.75 A SWG-L·5m | — | — | — |
| 09-11 00:41 | 9 | 29173.25 | 29170.54 A VWAP+2σ | S3 | S1:filled,S1:armed,S3:armed | S3 |
| 09-11 00:45 | 9 | 29185.5 | 29196.00 A ONH | — | S1:filled,S1:armed,S3:armed | — |

The top-ranked level carried a scenario on **2 of 13** reads and an ARMED scenario on **1** (00:41, S3 at 29170.54 VWAP+2σ vs its anchor 29171.75). That is the population this wave trades.

**C4 — the permission label is on the record.** Since W2's boot: n=160, permitted 114, excluded 46 (all `ib_held`), NULL 0. First rows: ids 5179, 5189, 5210, 5211, 5223, 5224 permitted (evaluated 19:06–19:23 CT); 5231, 5232 excluded `ib_held` (18:50, 19:01). W2 had booted (18:47:07).

**C5 — the first obstacle.** 113 scenarios since 09-08: 60 carry `economics.first_obstacle`, 53 do not (pre-contract docs). Obstacle R (n=53): p25/p50/p75 = 0.61/0.94/1.41; R<1: 31; R<2: 49. Recent ledger rows 131–154: 24 with an obstacle, 0 ≥ 2.0.

**C6 — one position per account** [A]: `trader/one_contract.go` refuses at the broker's book — an open position OR a working entry order (or a dark book) refuses; D4 is about which ARM, not how many positions.

**C7 — the map is uncapped** [A]: `PlanHardMaxLevels = 12` (`kernel/plan_doc.go:380`); `BuildMapCandidates` never drops a reference; `EntryShortlist` filters `EntryCandidate` only. E0's golden: `kernel/testdata/identity_legacy_output.json` (the level block via `RenderMapBlock` + the seated table).

**C8 — the tape is one contract** [A] at 00:46: ring 1500 bars 09-09 22:47 → 09-11 00:46, zero discontinuities > 60 pt, AddOn subscribed `MNQ 09-26`, store 09-26 = 51,284 / 12-26 = 7. At 00:16 (abc420f8) it was NOT: 609 bars below 29200 spanning 09-10 07:31→21:14 under the 12-26 label, jump 21:18 29132→29406.5, boot bar 00:15 open 29159.75 close 29445.75 — relayed to the bar-source lane (msg `ce6ab0f1`).

**C9 — hold-vs-break baseline** (since W1's boot; all rows; `validity=valid` rows are only 11 — line levels carry no formation time):

| side\|kind | hold | break | ambiguous | n |
|---|---:|---:|---:|---:|
| above|DEMAND | 3 | 1 | 1 | 5 |
| above|ONH | 1 | 6 | 4 | 11 |
| above|ONL | 2 | 0 | 1 | 3 |
| above|OR-H | 3 | 1 | 1 | 5 |
| above|PDC | 4 | 2 | 1 | 7 |
| above|PDL | 1 | 0 | 0 | 1 |
| above|SWG-H | 10 | 6 | 9 | 25 |
| above|SWG-L | 16 | 2 | 11 | 29 |
| above|VWAP | 22 | 17 | 40 | 79 |
| above|VWAP±2σ | 10 | 7 | 3 | 20 |
| above|eVWAP | 0 | 0 | 1 | 1 |
| below|DEMAND | 1 | 0 | 2 | 3 |
| below|FVG | 0 | 1 | 0 | 1 |
| below|IFVG | 0 | 1 | 0 | 1 |
| below|OB | 0 | 1 | 0 | 1 |
| below|ONH | 12 | 3 | 4 | 19 |
| below|OR-H | 1 | 1 | 2 | 4 |
| below|PDC | 1 | 1 | 2 | 4 |
| below|SWG-H | 10 | 9 | 13 | 32 |
| below|SWG-L | 0 | 8 | 5 | 13 |
| below|VWAP | 15 | 27 | 16 | 58 |
| below|VWAP±2σ | 4 | 4 | 3 | 11 |
| below|eVWAP | 0 | 0 | 1 | 1 |
| below|pdVWAP | 0 | 1 | 0 | 1 |

By side: **above** hold 72 / break 42 / ambiguous 72 (n=186); **below** hold 44 / break 57 / ambiguous 48 (n=149). Level tf is not a column; kinds stand in. Day zero.

## D — what shipped

- **D0** the map untouched — `kernel/one_setup_map_pin_test.go` (structural + the identity golden), green before E1 was written.
- **D1** `kernel.OneSetupAllowsAt(now, sc, level, perm, cfg)` — pure; LEVEL (grade≥min first, distance second, projections never; identity by id or merged id, else 3.0 pt), PLAY (`reject`), PERMISSION (NULL never permits). Unresolved never resolves by nearest.
- **D2** one call site (`oneSetupVerdictsAt`, once per cycle before the loop) + one consult (`oneSetupConsult`, after the class-48 gate, before the row). Declined → all three verdicts logged, episode rows stamped, counted, `continue`. **Never a cancel** (MANUAL-CANCEL-WINS); `declined_while_resting` counted.
- **D3** `leg.Target` = the recorded first obstacle, composed right after `composeArmStop` so the R:R gate judges it; refusal = the existing `rr` refusal + `obstacle_below_floor`; missing/wrong-side → authored target stands + `obstacle_missing`.
- **D4** `kernel.OneSetupOrder` (allowed first, by quality) + `second_setup_waiting` while another scenario holds a non-terminal row (the code's predicate `IsTerminalArmState`).
- **D5** `one_setup_enabled` (*bool, nil=ON [O]) · `one_setup_min_grade` (B [O]) registered live; `ResolveOneSetup` names the source.
- **D6** `kernel.ComputeFollowPlan` + `recordFollowPlans` at the detector hook (cadence = the planner read's, 30–120 min live) + `BackfillFollowPlans` through `WindowContract`/`BarsBetweenOn`.
- **D7** E9: recorder on/off → arm path byte-identical; the file names no placer (text pin) — mutant killed.
- **D8** `OneSetupBootLine` beside W2's in main.go; every field read. **D9** two three-state backfills (verdicts since W2's boot at each episode's OPEN against the recorded pool read + W2's stamp; follow-plans since W1's boot).

## E — RED / GREEN / mutations

- **E0** green on the untouched tree: `go test ./kernel -run 'TestOneSetupMapStaysWhole|TestIdentityLegacyOutputParity'` → `ok`.
- **E1/E3/E4 RED**: `kernel/one_setup_test.go:22:52: undefined: OneSetupLevelFacts` (+6) — no predicate existed. GREEN after `kernel/one_setup.go`.
- **E8 RED**: `kernel/follow_plan_test.go:33:39: undefined: FollowPlanInput` / `:51:8: undefined: ComputeFollowPlan`. GREEN after `kernel/follow_plan.go` (one fixture expectation of mine was wrong — MAE10 is 0 when the tape never goes against the short; fixed in the fixture, not the code).
- **E2** `trader/testdata/one_setup_arm_path.golden` generated by `TestOneSetupArmPathGoldenFixture` in a scratch worktree at dev `499e4f83` (both fixture rejects armed, counters empty); `TestOneSetupE2OffIsTodayByteIdentical` reproduces it under OFF byte for byte.
- **E1-seam · E3-seam · E5 · E6 · E7 · E9 · E10** drive `maybeManageArmedOrdersAt` on the real path with a known map via `oneSetupFactsForTest` (nil in production — pinned). Their RED is the mutation matrix below (each was written after the seam; the pins were then proven by breaking the seam).
- **E13 mutations** via `scripts/mutate.sh` (`-data/mutations.txt`): **12 applied, 12 killed** — D1 level/play/permission, D2 consult removed, D3 substitution removed, D4 rank order removed, D4 waiting removed, D6 forming-bucket-as-break, D6 touch-fills, D7 placer routed into the recorder, D7 recorder write removed, E7 stamp removed. One initial-value mutant (`through := false→true`) was EQUIVALENT (overwritten two lines later) and is recorded as SURVIVED-equivalent, not as a weak pin; the rule mutant was killed. One first-draft mutant did not compile and the harness said so.
- **E11** A29 gate: 16 names registered (wrappers included), every one with ≥1 production caller.
- **E14** full Go suite · vitest · tsc at the MERGED head: see §F.

## Backfill (D9) — counts at boot: see §F.

## A15 — recorded, not fixed

1. W1's link: `ParsePlanDoc` on the stored doc (class 120) — 101's.
2. `level_id` NULL on every live `touch_outcomes` row (0 of 999) — 105's stamp reaches no row live.
3. ASIA v7–v9 (09-10 21:29, 22:10, 22:49) were authored on the two-scaled tape; their levels are December-scale; the first plan after this boot is the first on a clean tape.
4. 44 of 78 multi-scenario plans never reached the ledger at all.
5. The follow-plan's cadence is the planner read's; a retest is recorded at the next read, not the next tick.
6. Round 17 not on dev.

## Pre-registration

- **E3 (one-setup)**: the null is that the one-setup filter changes nothing measurable at n<30 armed; the first comparison is `reject`-only pnl_corrected per session-day ON vs the C2 cell, reported with n and ids, no verdict before 30 arms.
- **Round 17's test**: null — role reversal ≤ 50%, follow ≤ 0 net after 2-pt friction. Cell criterion — approach direction (above/below) × level timeframe, ~385 episodes per cell, pooled first, then per cell: 95% lower bound of the role-reversal share above 50% AND of the follow's net above 0, at BOTH horizons (10 and 20 five-minute buckets). Only then does the follow side reach the wire — by a new wave, never by this recorder.

## Rollback

`one_setup_enabled=false` on the bound strategy restores the wide book without a boot (the seam reads the resolved config per cycle) — byte-identical per E2. Binary rollback: A13 backup named by the rev it holds, in `~/nofx-backups/bin/`, RELEASE + `mv` + kill.

## F — CUTOVER + PROOF

(filled at the kill)

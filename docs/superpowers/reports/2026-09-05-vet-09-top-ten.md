# Veteran deep review — section 09: top ten and owner rulings

Owner: hoang · 2026-09-05 · session `codex-vet-09-0905` · branch `docs/vet-09-0905` · documentation only.

## One-page summary

**My verdict:** I would keep this a one-contract SIM research book. I cannot sign off an edge, a live-money migration, or a size increase. I independently recover **58 eligible trades across 12 CME session-days**, corrected P&L **−$466.43**, mean **−$8.04 per trade**, and a descriptive normal-approximation 95% mean interval **[−$35.04, +$18.95]**. There are 18 winners, 38 losers and two flats; excluding flats, the win estimate is **18/56 = 32.1%, Wilson 95% [21.4%, 45.2%]**. The payoff is **1.75**, gross because recorded fees are zero. This is absence of demonstrated edge, not proof that the shape can never work. These trades also precede the first enforced strict-mode boot; I will not call them a strict-era performance test. [T] Full IDs, SQL, exclusions and day membership: `2026-09-05-vet-09-top-ten-data/q03_eligible.txt`; strict chronology: `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:423`.

**My three biggest problems:**

1. **An order can be called working without being the intended order.** The stop-entry call puts its trigger in the limit-price argument and sends zero as the stop price. Its Go wrong-side check also applies limit semantics to stops. Historical broker snapshots independently show nine one-contract order records for one scenario. A quantity clamp on each ticket is not a cap on combined contingent exposure. These are documented defects and historical observations; I did not submit orders or change them. [A] `ninjascript/VLTraderTCPClient.cs:976`, `trader/armed_executor.go:940`, snapshot IDs 1604, 1608, 1688, 1689 in q01.
2. **The evidence used to choose the edge is contaminated.** I reproduce 677 touch rows but only 423 distinct `(kind, price, opened_at_ms)` keys; RTH-L has 140 rows and just 14 such keys. Its scan is not cut at formation and its watermark only checks the current session day. Counting those copies as independent observations cannot justify flipping RTH-L or retiring six level kinds. [A] q01; `trader/detector_record.go:57`; `store/touch_outcomes.go:254`.
3. **The operating picture can be green while essential inputs are missing.** Health reports process availability, not a healthy tradable feed. The saved risk master and daily-loss toggle are off; the code on dev includes daily entry blocking that is absent from the running revision. Monitoring finds an in-app P0, not proof of owner receipt. [A] q01; `docs/superpowers/reports/2026-09-05-vet-04-monitoring.md:67`; `docs/superpowers/reports/2026-09-05-vet-04-monitoring.md:111`; `kernel/risk_limits.go:165`.

**My three biggest opportunities:**

1. Make a SIM ticket prove its entire lifecycle at the broker: correct trigger, one intended entry, protective bracket after fill, acknowledged cancellation, reconciled exit.
2. Produce an honest dataset with level formation, immutable scenario/leg identity, real commissions and stop/target provenance; then test targets and entry forms out of sample.
3. Simplify the model's authority: a planner that proposes an executable thesis, mechanical risk and order handling, and an executor prompt that accurately states strict mode. Remove contradictory instructions before adding another play.

Those are proposed future projects, not changes made during this audit. I use the requested veteran-trader perspective as an analytical lens, not as a claim that I personally ran a desk. [A] means code/output I checked; [B] is an inference. [T] is own-tape evidence with its population. [I] is judgment, untested here; it is not a claim of personal career history. [R] is reserved for a named market study and never borrowed from another instrument as proof of an MNQ edge. Official API and contract specifications below are technical references, not market-edge studies.

## 1. What I received, what I verified, and which version I mean

I claimed section 09 with `NOFX_SESSION=codex-vet-09-0905 deploy/nofx-claim.sh new docs/vet-09-0905`. The isolated worktree was cut from fetched `origin/dev` **2a66d91c**, rather than the older main checkout. I did not acquire, release or modify the main-tree lock. Existing claims for sections 01–08 and 10 were respected. Seven contained substantive reports; 04 stopped after its premises and 05 contained evidence but no report. Separate reviewers continued 04 and 05 on their existing branches. I integrated only report-directory paths and retained the earlier authors' reports as source material, including disagreements I reject below.

**Source availability and merge receipt:** see the final integration receipt and `q02_source_manifest.json` beside this report. They identify exact branch tips and whether each required report exists. Section 09 is not complete until sections 01–08 have landed on remote dev, their final versions have been read, this synthesis has been merged documentation-only, and the closing health GET returns 200. An HTTP 200 is not a market-data or safety certification.

**Specifications at acceptance:** `docs/superpowers/SYSTEM-MAP.md` last changed at **a96224dd**, September 4 09:07:37 CT; `docs/superpowers/AUDIT-CHECKLIST.md` at **15340faa**, September 4 13:22:07 CT. The requested `docs/superpowers/plans/VL-MASTER-PLAN-v2.md` is absent at this tip; I do not invent a replacement. Checklist numbers are labels: PART 1 has 78 numbered entries, highest 79, with 46 absent; a later CLASS 75 header uses another numbering series. I cross-check actual text, not a claimed count.

**Runtime:** my fresh health GET at **15:52:37 CT** returns 200, revision **36648655cfe0**. Dev base 2a66d91c includes the already-written risk/bias wiring merge. The trading runtime was not restarted or deployed. I use current source lines for implementation and explicitly distinguish running behavior when those revisions differ.

**Population:** entry time at or after **2026-08-15 00:00 CT**, epoch milliseconds `1786770000000`; exclude test source `e7_farside_test` (572–574), `plan_id='UNRESOLVABLE'` (530, 539, 545, 546, 566, 571, 580), and null corrected P&L (576, 577, 579). The resulting **58 IDs** are listed individually in `q03_eligible.txt`. No pre-era trades enter this population. Session-days roll at 17:00 CT using timezone-aware conversion. **q03 supersedes q01's incorrectly named “Canonical era”: q01 missed the attribution sentinel by searching correction notes.** Sections 01, 05 and 06 also use the broader 65-row population; their performance/exit/excursion rates are not compliant primary estimates under this dispatch. The seven sentinel rows sum to −$97.50, exactly explaining the broader −$563.93 total versus eligible −$466.43. Retained broader results are audit-history comparisons, not the accepted performance population. Section 04's corrected EOD population independently agrees at 58. All parent SQL uses `mode=ro`, query-only and a read transaction.

**Uncertainty:** Wilson intervals below describe binomial proportions and do not cure correlated episodes, selection bias or repeated testing. Expectancy needs a mean interval and day-cluster sensitivity, not a Wilson interval. Section 06's day-bootstrap interval **[−$34.29, +$18.97] per trade**, n=65 in 12 days, is a broader-population simulation, not an interval for the compliant 58-row cohort. I do not adopt its drawdown or bootstrap numbers as calibrated policy thresholds. `docs/superpowers/reports/2026-09-05-vet-06-risk.md:83`. A zero-filled strict-era book is not a profitable book.

**Source limitations:** `trade_excursions` is empty. Recovering MAE/MFE from position columns, authored stops or bars is a proxy with separate missingness and fill-order assumptions. Static snapshots of protected API endpoints are identified in the section reports; I independently refreshed public health and read bound config from the store. I did not run `cmd/gate-jwt` against the writable production DB: it calls `store.New`, which initializes schemas and default data (`cmd/gate-jwt/main.go:50`, `store/store.go:62`). The audit's DB-read-only rule wins over treating a token helper as harmless. No secrets are included in this report.

## 2. Recommendations — my ten changes, in order

### 1. Prove and repair the stop-entry contract before relying on reclaim arms

**What:** a future execution fix must assign the stop trigger to the correct NT8 argument, use a stop-specific crossed-trigger predicate, and verify the accepted order's type, trigger and quantity. Include both long and short, equality, correct-side, crossed-side, rejection, reconnect, fill and post-fill bracket cases. The present audit only records this need.

**Why:** [A] `ninjascript/VLTraderTCPClient.cs:972` constructs a StopMarket but `:978` supplies `orderPx, 0`; `trader/armed_executor.go:940` calls the limit predicate whose short branch returns `price > entry` at `:993`. The official [NT8 CreateOrder reference](https://ninjatrader-staging.ninjatrader.com/support/helpguides/nt8/createorder.htm) specifies separate limit and stop arguments. Section 08 supplies the far-side log witnesses; q01 independently confirms the malformed-price snapshot shape. `docs/superpowers/reports/2026-09-05-vet-08-stretch.md:172`.

**What it takes:** code plus a separately authorized C# deployment and SIM acceptance run. **Number I watch:** count of submitted stop entries with zero/wrong echoed trigger, count of invalid-side submissions, and positions lacking an acknowledged protective stop; required acceptance result zero. A rejected or inert SIM stop is not evidence that an executable live stop would be safe.

### 2. Enforce the position-and-pending-order budget at the broker boundary

**What:** before placing or replacing an entry, reconcile broker working entries and pending cancels against immutable intent/scenario/leg identity. A cancel request is not a cancellation. Reserve the one-contract exposure budget across all entries that could fill, including simultaneous scenarios and split legs; protective exits must not be mistaken for new entries. Fail closed on stale or unknown broker state while preserving exits.

**Why:** [A] snapshots 1604/1608/1688/1689 contain nine order records with one contract each at the same trigger. Some are Initialized or CancelSubmitted, so I do not call them nine actual filled positions. Their unresolved contingent exposure is the finding. The old silence-reaper was fixed by class 79; this proposal is broker-aware placement and replacement reconciliation, not a request to repeat that fix. `trader/armed_executor.go:905` to `:972`; q01.

**What it takes:** code, a stable identity contract, and simultaneous-fill/cancel-ack/reconnect fixtures. **Number I watch:** maximum possible filled-plus-pending entry exposure versus the one-contract budget, and orphan orders per logical intent. The evidence gate is zero breaches under adversarial SIM tests, not one quiet snapshot.

### 3. Make the loss boundary and the desk's operating state truthful

**What:** separately verify the already-merged daily entry-blocking change in the running binary before treating the configured $450 limit as protection. Design prompt-free checking, pending-entry cancellation on trip, restart persistence and an explicitly stated policy for already-open risk. Display feed/link/snapshot age, corrected session P&L, master/limit state and broker stop coverage. Use existing in-app and NT8 surfaces; a new alert channel is outside the owner's ruling.

**Why:** [A] q01 finds master off and daily loss off, configured $450. `kernel/risk_limits.go:165` explicitly says the new latch blocks entries and does not close open positions; the name ForceFlat is not its behavior. Monitoring distinguishes a process response from flowing bars and documents existing alerts. `docs/superpowers/reports/2026-09-05-vet-04-monitoring.md:87`; `docs/superpowers/reports/2026-09-05-vet-06-risk.md:60`.

**What it takes:** owner risk-policy ruling, verification of existing code before any new code, then UI/monitor wiring. [I] If the owner wants candidate budgets for research, section 06's **$150 per trade and $900 per week** are risk preferences to test, not calibrated optima. I retain the existing no-trade-count-cap ruling. **Number I watch:** time from trip to acknowledged entry cancellations, stale-data exposure duration, and actual risk versus the explicitly enabled limits. A nominal stop cap is not a guarantee against gaps or slippage.

### 4. Repair measurement before promoting, retiring or flipping a level

**What:** preserve old rows as legacy evidence; write formation-aware, idempotent touch episodes with versioned definitions and source bars. Record true score components for both seated and cut candidates. Separate RTH, Globex and calendar-day references explicitly, check coverage before emitting an anchor, and name the owner's chosen session convention.

**Why:** [A] q01 reproduces 677 rows/423 price-time keys, RTH-L 140 rows/14 keys and 360 candidate rows with empty component objects. The recorder scans scope bars without a formation cutoff while the watermark is session-scoped. `docs/superpowers/reports/2026-09-05-vet-02-levels.md:34`; `docs/superpowers/reports/2026-09-05-vet-02-levels.md:35`. I do not infer edge from ordinal counts whose ordinal resets with an observation bug.

**What it takes:** code and data first; owner ruling on anchor semantics where the existing contract intentionally uses calendar days. **Number I watch:** duplicate logical episodes, episodes predating formation, incomplete anchor windows and missing component records. Required measurement acceptance: zero known defects, with old/new eras kept separate. This is not a proposal to tune grading weights.

### 5. Remove target inflation as a way to pass the R:R floor

**What:** generate candidate geometry from a named structural target first; compare it with prospective excursion evidence. If that target cannot clear the floor after the actual stop is composed, the setup is infeasible. Record that outcome. Do not move the farthest target to the front merely to make the gate green. Keep revalidation at placement and whenever orders are repriced or risk changes.

**Why:** [T] section 01 reports 47/132 authored-arm scenarios in [2.0, 2.3) planned R:R: **35.6%, Wilson [28.0%, 44.1%]**, IDs/plan keys in its q11 scenario corpus. [A] section 03 traces repeated S2 geometry changes across ledger IDs 62–102; the denominator of ledger rows is not independent opportunities. `docs/superpowers/reports/2026-09-05-vet-01-way-it-trades.md:50`; `docs/superpowers/reports/2026-09-05-vet-03-decisions.md:41`. My gross payoff of 1.75 on 56 decided eligible trades is not comparable to a model's scenario ratio without identifying both populations.

**What it takes:** data first, owner target-ownership ruling, then code. **Number I watch:** net expectancy, target reach before stop, and arm replacement cycles per unchanged thesis. Compare structural, fixed-R and MFE-informed candidates on a held-out chronology with fees and pessimistic ambiguous-bar handling. [I] No MFE percentile is endorsed for execution from the present selected sample.

### 6. Repair authorability feedback so the planner has a legal next move

**What:** give the model the executable condition, direction, entry type and resolved floor at each candidate level. Repair advice must remain legal under strict mode and the arm validator; record failed read identity and revision so the next identical read can recognize the failure. Preserve fail-closed behavior.

**Why:** [A] the September 4 reject sequence 118–120 lost the NY scheduled read; 119 offers immediate entry and 120 rejects its arm. Subsequent sequences 124–126, 127–129 and 130–132 repeat related failures. `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:35`; `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:226`. An earlier void-breakdown defect was already cured; the incompatible repair path is a distinct residual issue. I do not combine successful plan versions, transport errors and repeated re-author attempts into an invented overall rejection probability.

**What it takes:** prompt/validator-renderer code and production-boundary tests. **Number I watch:** failed scheduled reads, repeated same-rule failures per read, time to executable plan, and whether each repair suggestion can actually pass the existing validator. No market belief is required for this engineering change.

### 7. Remove entry authority from the strict-mode executor's prose, then test event-driven calls

**What:** eliminate the invitation to open market positions when the resolved mode forbids them. Mark every scenario's actual path: confirm met with a resting order, confirm met without an executable arm, or not ready. Retain mechanical stop/target, reconciliation and liveness work independently of any reduction in AI calls. Trial event-triggered model review before removing a timer call used by other behavior.

**Why:** [A] section 07 rereads the thirteen strict refusals as the plan's own S1, not off-plan improvisation; the active scenario had no arm. These are **13 of 14 September 3 open proposals, 92.9%, Wilson [68.5%, 98.7%]**, with decision IDs 37304–37322 enumerated in q33. The repeated proposals are one thesis, not thirteen independent missed trades. `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:36`.

**What it takes:** prompt/renderer changes first; code and an explicit dependency audit for event-driven calls. **Number I watch:** impossible open proposals, model latency/cost, missed eligible events and protection/reconciliation latency. Token savings are useful but cannot prove trading improvement.

### 8. Remove stale market crowns and the unused FVG demand; rewrite the Rules paragraph

**What:** delete frozen unsourced win claims and weekday prescriptions, and suppress FVG-authoring demand while the condition is shadowed. Retain any deliberately required shadow research on a clearly separate surface. Replace the single Rules paragraph with short numbered rules derived from the actual mode and validator. The rewrite is an appendix, not applied configuration.

**Why:** [A] `kernel/planner_prompt.go:656`, `:660`, `:737` mix market claims with constraints; section 07 measures the old paragraph at 52 sentences and 2,480 approximate o200k tokens, prompt row 132. `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:124`. Shadowing FVG and breakout_retest is already implemented at `kernel/condition_status.go:26`; I am proposing removal of contradictory prompt demand, not re-shadowing them.

**What it takes:** prompt plus owner ruling on discretionary doctrine and shadow-authoring policy. **Number I watch:** unsupported numerical claims, mismatched authority statements, retained validator semantics, tokens and rejects per actual read. I would not paste section 07's current appendix as-is: it preserves capitalized guard fragments, calls breakout_retest a normal AI play under strict and changes behavior while claiming the same information. `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:515`; `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:562`. Approval of a structural rewrite is not approval to weaken validators.

### 9. Make the record of a trade and its review complete

**What:** at close persist the broker exit cause, actual acknowledged initial stop/target, excursions with provenance, fees and entry/exit slippage. Link read facts to plan/version and distinguish unknown token counts from zero. Produce a cutoff-correct 15:00 CT review of realized P&L, unresolved rows, broker/ledger differences, arms, refusals, reads and outages.

**Why:** [A] q01 shows an empty excursion table, zero recorded fees, and read-fact rows 1–32 with empty AI/tree labels and zero tokens. Monitoring supplies seven reproducible EOD sections and explicitly distinguishes data known at cutoff from later acknowledgement state. `docs/superpowers/reports/2026-09-05-vet-04-monitoring.md:204`. **A fill minus an old authored stop is not necessarily slippage:** the comparison must use the actual broker-acknowledged stop applicable to that fill. Section 05 Q1 verifies position 591's broker stop and fill both at 29355: **zero broker-referenced stop slippage**. The 3.3715-point difference against the later ledger stop is geometry drift. Section 04's desk-strip example and section 08's slippage label must not be used as execution-cost evidence; section 05's raw NT8 line receipts supersede them.

**What it takes:** code/data; clarify broker fields and avoid retrospective overwrites. **Number I watch:** missing stop/exit-cause/fee provenance, impossible joins, unresolved corrected P&L and day-review coverage. A near-match to a minute bar cannot establish queue quality, partial-fill behavior or stop-market execution under live liquidity.

### 10. Run one prospective experiment, with separate evidence and operational gates

**What:** after execution and measurement pass, freeze one primary hypothesis and a small preregistered comparison of entry/target forms. Record all eligible opportunities, their fillability and rejected/missed events with a stable key. Stratify by session/regime only where enough independent days exist. Keep unpromoted ideas shadow-only; do not force a trade because the model wrote a bullish label.

**Why:** [T] the only independent unit for day-level uncertainty in this record is 12 observed days, not dozens of overlapping report slices. Section 10's raw RTH-L prior is invalidated by the duplicate/formation finding; a deadline cannot guarantee enough evidence. `docs/superpowers/reports/2026-09-05-vet-10-ideas.md:150`; `docs/superpowers/reports/2026-09-05-vet-06-risk.md:83`.

**What it takes:** owner hypothesis/loss-budget ruling, clean data and a reproducible net-of-cost replay before any implementation choice. [I] I would require at least **30 clean prospective session-days and 100 resolved trades in the unchanged intended policy** before even reviewing a size change, plus a positive day-cluster lower confidence bound after costs and acceptable owner-defined drawdown. Those are minimum review gates, not a promise that the sample will be conclusive; sparse cells stay unpromoted.

**Number I watch:** net expectancy lower bound, number of independent days, friction sensitivity, drawdown and exposure-control breaches. At the one-month review: stop funding a defective measurement or unsafe execution mechanism; if the clean sample is merely too small, record inconclusive and remain SIM/shadow. Failure to prove an edge by a calendar date is not proof of no edge.

## 3. Where I disagree with the reports

1. **Section 01's “cannot make money on this tape as configured” is too broad as a predictive verdict.** Its broader-population historical loss is reproducible but includes seven forbidden sentinel rows; the confidence interval crosses zero, and the latest strict policy has no post-enforcement trade sample here. I adopt “no demonstrated edge,” not impossibility. Section 01 also says 14 session-days; the explicit 17:00 CT bucket returns **12**. `docs/superpowers/reports/2026-09-05-vet-01-way-it-trades.md:46`; q03 eligible IDs; the original broader 65-row day count also equals 12.
2. **Sections 08 and 10 validate a raw RTH-L break rate that section 02 shows is not independent.** Raw **82/124 = 66.1%, Wilson [57.4%, 73.9%]**, ambiguous 16 of 140, is a faithful row count but not an admissible edge estimate. There are only 14 price-time keys and formation-time leakage. Even the dedup alternatives differ (section 01 majority outcome versus section 02 price/side treatment). I refuse to crown any of them. `docs/superpowers/reports/2026-09-05-vet-08-stretch.md:54`; `docs/superpowers/reports/2026-09-05-vet-10-ideas.md:59`; `docs/superpowers/reports/2026-09-05-vet-02-levels.md:53`; q01 names every raw RTH-L row.
3. **Section 03's “judge R:R once at write” is not enough.** The stop floor, price and available target can move before placement. Compose consistent geometry at write, keep mechanical checks at placement/change, and make infeasibility terminal for that unchanged intent until a legitimate new observation justifies replacement. `docs/superpowers/reports/2026-09-05-vet-03-decisions.md:191`.
4. **Section 08's replay is not an exact current-rules replay.** It explicitly uses corrected stop semantics, approximated invalidation and closed-bar timing that differ from production. Its **−144.71 points in three modeled fills**, versus **−76.83** under one changed cancellation, is a scenario sensitivity, not achieved P&L and not a rigorous bound. Modeled identities are in its replay_A_arms.csv; observed IDs 587–591 total **−$521.50**. `docs/superpowers/reports/2026-09-05-vet-08-stretch.md:150`; `docs/superpowers/reports/2026-09-05-vet-08-stretch.md:162`. The single hindsight continuation winner is a mechanism illustration, not evidence to enable a play.
5. **Section 08's suggestion to choose the farthest target to satisfy R:R reverses the evidence test.** Twelve of 24 reclaim candidates pass when a farther target is selected (**50.0%, Wilson [31.4%, 68.6%]**), but passing a constraint is not earning the target. I will not turn that statistic into permission. `docs/superpowers/reports/2026-09-05-vet-08-stretch.md:198`.
6. **Section 10's one-month automatic level promotion is premature.** A touch-hold interval and median excursion cannot substitute for executable net expectancy; adjacent touches are dependent; ambiguous episodes are part of the instrument's behavior. The raw NY RTH-L prior and its assumed rate of sample accumulation are contaminated. A predeclared end date should review evidence, not force a winning or losing label. `docs/superpowers/reports/2026-09-05-vet-10-ideas.md:150`.
7. **The time-stop recommendations conflict and neither proves its causal claim.** Section 01 expects a no-progress scratch to spare winners; section 10 says full-stop losers mean a time stop changes nothing. Both require the ordered intratrade path and a pessimistic replay. End-of-trade MAE/MFE cannot answer either alone. `docs/superpowers/reports/2026-09-05-vet-01-way-it-trades.md:57`; `docs/superpowers/reports/2026-09-05-vet-10-ideas.md:222`.
8. **“No alert” and “unseen alert” are different claims.** The monitoring completion recovers alert IDs 629 and 654 and corrects its first draft's inference about receipt. I keep the delivery/visibility gap and do not claim to know what the owner saw. `docs/superpowers/reports/2026-09-05-vet-04-monitoring.md:181`.
9. **A higher minimum trade count is not a statistical cure.** I retain Stage-A size one. Raising a gate from 30 to 100 does not solve selection, serial dependence, repeated peeking or an unmeasured friction penalty. I would not adopt precise Monte Carlo percentiles as optimized production kill thresholds. Section 06's 100-trade p95 drawdown around **$2,822** is a simulation conditional on the 65-trade/12-day sample, not a guaranteed capital requirement. `docs/superpowers/reports/2026-09-05-vet-06-risk.md:83`.
10. **The proposed Rules appendix is review material, not deployment-ready.** Section 07 itself identifies guard rewrites still needed, retains ALL-CAPS fragments, and changes authoring policy. The owner can approve shortening without approving any of those policy changes. `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:562`. I would require a meaning-preserving draft first, then separate explicit rulings on removals.

## 4. Rulings page — yes/no questions for the owner

These are proposed decisions for a later change dispatch. Nothing in this page is applied by the audit. “Yes” approves the described future work, not a trade or automatic deployment. [I] denotes untested policy judgment; numbered references point to the evidence-backed recommendations above.

| Owner-only decision, phrased yes/no | My answer | One-sentence reason |
|---|---|---|
| Keep this system SIM-only at one contract? | **Yes** | No positive net edge or fully proved execution chain supports live money or size now (§1, §10). |
| Authorize a separate stop-entry/bracket contract repair and far-side SIM acceptance dispatch? | **Yes** | The trigger is in the wrong NT8 slot and the Go predicate uses limit semantics (§1). |
| Require a one-contract budget across positions plus all potentially fillable entries, including pending cancels? | **Yes** | Per-ticket size did not prevent nine simultaneous order records (§2). |
| Verify the already-merged daily-loss wiring in a separately authorized deployment before believing the configured $450 boundary? | **Yes** | It is absent from the running revision and the saved master/limit flags are off (§3). |
| Make a daily loss trip cancel resting entries and persist through restart, while preserving protective exits? | **Yes** | Blocking the next AI cycle alone leaves contingent risk (§3, policy [I]). |
| Make $450 a guaranteed maximum realized loss? | **No** | Fills, open risk and gaps can overshoot; choose explicitly whether a trip also requests flattening (§3). |
| Use $150 initial per-trade risk and $900 weekly loss as candidate SIM budgets for evaluation? | **Yes, provisional** | These are owner risk preferences to test for feasibility, not fitted edge parameters (§3, [I]). |
| Add a trade-count cap? | **No** | The existing owner ruling remains; no new evidence here overrides it (§3). |
| Allow bounded marketable entry up to four MNQ ticks now? | **No** | Keep cancel/no-chase behavior until correct stop semantics and fill-quality measurement exist (§1, §5). |
| Test four ticks as a shadow-only marketable-entry cap? | **Yes, provisional** | Four ticks equal one point or $2 per contract of additional entry displacement, excluding all other costs; it is a comparison hypothesis, not calibration ([I], CME specification). |
| Retire eVWAP, VWAP±2σ, EQL, ONH, ONL and PDL on the existing small-n touch statistics? | **No** | The measurement is duplicated and formation-leaking; keep measuring and distinguish screen clutter from edge failure (§4). |
| Flip RTH-L automatically from fade to break? | **No** | The apparent large denominator collapses to 14 price-time keys (§4). |
| Remove the FVG demand block while FVG is shadowed? | **Yes** | Do not ask the trading planner for an unexecutable primary play; retain separately named shadow research if desired (§8). |
| Replace the Rules paragraph with a short numbered, semantically checked instruction appendix? | **Yes** | The current appendix still needs policy and validator reconciliation before use (§8). |
| Add a 14:45–16:30 CT blackout for nonessential planner event wakes? | **Yes, policy proposal** | Preserve risk monitoring, reconciling and the 16:30 scheduled read; this is a new explicit budget rule, not a proven P&L improvement (§10, [I]). |
| Change the 14:45 CT flatten time now? | **No** | One hindsight trend day is not evidence to change the holding horizon (§10). |
| Make acceptance and hold armable now? | **No** | They have no current arm kind; define time-confirm completion and order semantics, then shadow-test before enabling (§6, `kernel/arm_kind.go:36`). |
| Replace model-authored targets directly with a fitted MFE percentile now? | **No** | The empty excursion table and selective stop recovery do not justify a production percentile (§5). |
| Test structural targets and MFE-informed targets against fixed-R targets out of sample? | **Yes** | This tests executable reward rather than rewarding the model for satisfying the floor (§5). |
| Require a directional trade because the AI/tree label is long or short? | **No** | Calibrated labels are not a proven permission signal; executable symmetry is different from forced exposure (§7, [I]). |
| Remove impossible strict-mode open instructions and stale numerical market claims? | **Yes** | They contradict enforced authority and the evidence contract (§7–8). |
| Disable ASIA or all long trades solely from this retrospective sample? | **No** | It mixes policies and thin correlated subgroups; no new session ban is proved (§10). |
| Require a short pre-session owner review with a veto and existing emergency controls? | **Yes** | Human judgment should confirm operability and risk; an approval button's existence is not proof it gates orders (§3, [I]). |
| Add a new external alert channel? | **No** | The owner's declined-channel ruling stands; test existing in-app/NT8 warning visibility first (§3). |
| Retire the fade book automatically if no cell is statistically promoted by the one-month date? | **No** | Insufficient data is inconclusive; operational failure and adverse net evidence are distinct stop criteria (§10). |

Technical tick arithmetic only: the [CME Micro E-mini specification](https://www.cmegroup.com/articles/faqs/frequently-asked-questions-micro-e-mini-equity-index-futures.html) gives MNQ $2 per index point and a 0.25-point outright tick, or $0.50. A four-tick cap limits additional entry displacement relative to a defined reference; it does not cap total trade loss. No market-entry, stop-limit or stop-market variant is authorized for production by this table.

## 5. Cross-check against existing fixes and surprises

I do not re-recommend the class-79 snapshot-based reaper, the one-ATR parity repair, the existing strict gate, FVG/breakout-retest shadowing, the Stage-A one-contract clamp, BE/trail suspension, or the dev risk/bias call-site wiring. Their presence is evidence, not a reason to assume related paths are correct. The new work proposed here is stop-order semantics, broker-aware aggregate pending exposure, persistent risk response, truthful UI/data provenance, formation-aware measurement and coherent model authority. Sources: `docs/superpowers/AUDIT-CHECKLIST.md:1820`; `kernel/condition_status.go:26`; `trader/entry_gate.go:157`; `trader/exit_mechs_suspend.go:33`; section 07's already-fixed cross-check `docs/superpowers/reports/2026-09-05-vet-07-prompts.md:380`.

**Surprises recorded, not acted on:** the malformed stop entry and duplicate pending book; an operationally named ForceFlat latch that deliberately does not flatten; token helper schema/default initialization; the mismatch between saved risk settings and the running code; and empirical conclusions that change when the row identity or policy era is corrected. The empty excursion table and the absence of any observed post-strict fill mean a clean live-money verdict cannot be manufactured from these reports.

## 6. Completion receipt

Sections 01–08 and 10 are published on remote dev **55dde65e8256570ac97dd11d16e0a0f45e89fb54**. I verified each final report against the remote-dev blob by SHA-256; exact branch tips, filenames and line counts are in `q02_source_manifest.json`. Section 04 completion is e41f2c23 and section 05 completion is 56fca3d8. The integrated source packet changes **340 paths, all under docs/superpowers/reports**. This synthesis and its six evidence files are the subsequent documentation-only commit. The closing health GET returned **HTTP 200**; the exact body is in `q04_completion.json`. No application code, strategy/configuration, DB, service, order or runtime was changed by this lane. Application tests were not run for report-only edits; checks cover report availability, immutable source hashes, population calculations, source citations and path scope.

The remaining unproved items are explicit findings, not hidden passes: no strict-era realized sample, no populated excursion table, no queue/slippage distribution for live fills, no valid exact-production counterfactual, and no deployment-ready prompt rewrite. Source reports retain their original conflicting estimates; the population and broker-stop corrections above govern this synthesis. The final branch/dev push and removal of this lane's clean worktree are recorded in the scratch completion receipt after publication. Existing section-owner worktrees and the main-tree lock remain untouched.
